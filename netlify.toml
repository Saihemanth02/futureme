[build]
  publish = "frontend"
  functions = "netlify/functions"

# Redirect all API requests to Netlify serverless functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
